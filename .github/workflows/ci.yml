name: CI Pipeline

on:
  push:
    branches: 
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - name: Checkout Code
      uses: actions/checkout@v4
    - name: Build api docker image
      run: docker build . --file api/Dockerfile --tag api:latest
    - name: List all docker images
      run: docker image ls 
